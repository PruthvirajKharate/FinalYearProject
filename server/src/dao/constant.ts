export enum PROPOSAL_STATUS {
    PENDING = 'pending',
    ACTIVE = 'active',
    CANCELED = 'canceled',
    DEFEATED = 'defeated',
    SUCCEEDED = 'succeeded',
    QUEUED = 'queued',
    EXECUTED = 'executed'
}